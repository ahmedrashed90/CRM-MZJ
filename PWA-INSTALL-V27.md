# MZJ CRM Sales v27 - Installable PWA

- Added a valid web app manifest with standalone display.
- Added Android maskable icons and Apple touch icon.
- Added a service worker and registration script.
- Added iOS/Android standalone meta tags.
- No CRM/Firebase/business logic was changed.

After deployment, remove the old shortcut, clear the site's stored data if Chrome still shows the old icon, reopen the HTTPS URL, then choose Install app / Add to Home screen.
